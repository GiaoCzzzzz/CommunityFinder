<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CommunityFinder.Views.HistoryPage"
             x:Name="ThisPage"
             Title="History"
             BackgroundColor="#DEF685">

    <ScrollView>
        <StackLayout>

            <!-- ====================== Browsing History ====================== -->
            <Label Text="Browsing History"
                   FontSize="24"
                   HorizontalOptions="Center"
                   Margin="0,10"/>

            <Button Text="Clear All History"
                    BackgroundColor="#EF8687"
                    TextColor="White"
                    Margin="10"
                    Clicked="OnClearAllHistoryClicked"/>

            <CollectionView ItemsSource="{Binding BrowsingHistory}"
                            SelectionMode="Single"
                            SelectionChanged="OnHistoryItemTapped">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="10"
                               Padding="10"
                               HasShadow="True"
                               CornerRadius="8">

                            <!-- ✅ 使用 DataTrigger 根据 ItemType 改变颜色 -->
                            <Frame.Style>
                                <Style TargetType="Frame">
                                    <Setter Property="BackgroundColor" Value="#D5B7B6"/>
                                    <!-- 默认底色 -->
                                    <Style.Triggers>

                                        <!-- ✅ 课程项（COURSE）为淡绿色 -->
                                        <DataTrigger TargetType="Frame"
                                                     Binding="{Binding ItemType}"
                                                     Value="COURSE">
                                            <Setter Property="BackgroundColor" Value="#B6D7A8"/>
                                        </DataTrigger>

                                        <!-- ✅ 活动项（EVENT）为橙黄色 -->
                                        <DataTrigger TargetType="Frame"
                                                     Binding="{Binding ItemType}"
                                                     Value="EVENT">
                                            <Setter Property="BackgroundColor" Value="#F9CB9C"/>
                                        </DataTrigger>

                                    </Style.Triggers>
                                </Style>
                            </Frame.Style>

                            <!-- 卡片内容 -->
                            <StackLayout Orientation="Horizontal" VerticalOptions="Center">
                                <StackLayout HorizontalOptions="StartAndExpand">
                                    <HorizontalStackLayout Spacing="8" Margin="0,0,0,4">
                                        <Label Text="{Binding ItemType}"
                                               FontSize="10"
                                               BackgroundColor="#FFA726"
                                               TextColor="White"
                                               Padding="4,2"
                                               FontAttributes="Bold"/>
                                    </HorizontalStackLayout>

                                    <Label Text="{Binding Title}"
                                           FontSize="18"
                                           FontAttributes="Bold"/>
                                    <Label Text="{Binding Outlet}"
                                           FontSize="14"
                                           TextColor="Gray"/>
                                </StackLayout>

                                <Button Text="Delete"
                                        BackgroundColor="#EF8687"
                                        TextColor="White"
                                        Padding="10,5"
                                        Command="{Binding BindingContext.DeleteHistoryCommand, Source={x:Reference ThisPage}}"
                                        CommandParameter="{Binding .}"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>


            <!-- ====================== Favorite Courses ====================== -->
            <Label Text="Favorite Courses"
                   FontSize="24"
                   HorizontalOptions="Center"
                   Margin="0,10"/>

            <Button Text="Clear All Favorites"
                    BackgroundColor="#EF8687"
                    TextColor="White"
                    Margin="10"
                    Clicked="OnClearAllFavoritesClicked"/>

            <CollectionView ItemsSource="{Binding FavoriteCourses}"
                            SelectionMode="Single"
                            SelectionChanged="OnFavoriteItemTapped">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame Margin="10"
                               Padding="10"
                               HasShadow="True"
                               CornerRadius="8">

                            <!-- ✅ 根据 ItemType 切换颜色 -->
                            <Frame.Style>
                                <Style TargetType="Frame">
                                    <Setter Property="BackgroundColor" Value="#88ACBC"/>
                                    <!-- 默认底色 -->
                                    <Style.Triggers>

                                        <!-- ✅ 课程项（COURSE）为浅蓝 -->
                                        <DataTrigger TargetType="Frame"
                                                     Binding="{Binding ItemType}"
                                                     Value="COURSE">
                                            <Setter Property="BackgroundColor" Value="#A4C2F4"/>
                                        </DataTrigger>

                                        <!-- ✅ 活动项（EVENT）为粉紫 -->
                                        <DataTrigger TargetType="Frame"
                                                     Binding="{Binding ItemType}"
                                                     Value="EVENT">
                                            <Setter Property="BackgroundColor" Value="#EAD1DC"/>
                                        </DataTrigger>

                                    </Style.Triggers>
                                </Style>
                            </Frame.Style>

                            <!-- 卡片内容 -->
                            <StackLayout Orientation="Horizontal" VerticalOptions="Center">
                                <StackLayout HorizontalOptions="StartAndExpand">
                                    <HorizontalStackLayout Spacing="8" Margin="0,0,0,4">
                                        <Label Text="{Binding ItemType}"
                                               FontSize="10"
                                               BackgroundColor="#FFA726"
                                               TextColor="White"
                                               Padding="4,2"
                                               FontAttributes="Bold"/>
                                    </HorizontalStackLayout>

                                    <Label Text="{Binding Title}"
                                           FontSize="18"
                                           FontAttributes="Bold"/>
                                    <Label Text="{Binding Outlet}"
                                           FontSize="14"
                                           TextColor="Gray"/>
                                </StackLayout>

                                <Button Text="Delete"
                                        BackgroundColor="#EF8687"
                                        TextColor="White"
                                        Padding="10,5"
                                        Command="{Binding BindingContext.DeleteFavoriteCourseCommand, Source={x:Reference ThisPage}}"
                                        CommandParameter="{Binding .}"/>
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </StackLayout>
    </ScrollView>
</ContentPage>
